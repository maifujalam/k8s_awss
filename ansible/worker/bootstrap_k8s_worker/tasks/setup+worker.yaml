- name: Copy worker join templates
  template:
    src: join-default-worker.yaml.j2
    dest: /home/{{USER}}/{{PROJECT_PATH}}/v{{KUBERNETES_VERSION}}/{{PACKAGE_DIRECTORY}}/join-default-worker.yaml.j2

- name: Reset kubernetes
  become: true
  shell: kubeadm reset --force

- name: Init cluster
  become: true
  shell:  kubeadm join --config /home/{{USER}}/{{PROJECT_PATH}}/v{{KUBERNETES_VERSION}}/{{PACKAGE_DIRECTORY}}/join-default-worker.yaml.j2

